export class LookTargetController {
    static setTarget(target: string) {
        // Valid targets: User, Visualization, Highlighted Element, Notes Panel, Quiz Panel, Camera
        // Controls eye and head direction
    }
}
