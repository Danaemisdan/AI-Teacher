export class AnimationController {
    static playAnimation(animation: string) {
        // Wraps raw animation APIs
    }
    
    static stopAnimation() {
        // Stops current animation
    }
    
    static queueAnimation(animation: string) {
        // Queues an animation
    }
    
    static interruptAnimation(animation: string) {
        // Interrupts current animation and plays new one
    }
    
    static blendAnimation(animation: string) {
        // Blends to new animation
    }
    
    static setIdleAnimation(animation: string) {
        // Sets default idle animation
    }
}
